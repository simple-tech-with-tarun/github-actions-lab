name: caller

on:
  workflow_dispatch:


jobs:
  call:
    uses: ./.github/workflows/reusable-ci.yml    

    # call job has custom name.. not something fixed.. incorrect details added in last commit in the branch.
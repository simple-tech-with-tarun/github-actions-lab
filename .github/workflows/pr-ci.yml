name: PR Fail CI

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  pre-check:
    runs-on: ubuntu-latest

    steps:
      - name: codeowners test
        run: |
          echo "Running PR CI"
          echo "testing codeowners"

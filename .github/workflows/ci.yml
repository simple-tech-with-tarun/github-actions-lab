name: ci_1

on:
  workflow_dispatch: # running on manual trigger
  # push:
  #   branches: # runs after pushing to main branch
  #     - main
  # pull_request:
  #   branches: # runs after pull request targetting any branch
  #     - "**"

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:
      - name: test job
        run: echo "Test Job validated"
  
  build:
    needs: test
    runs-on: ubuntu-latest
  
    steps:
      - name: build job
        run: |
          echo "Building or not"
          exit 1

  deploy:
    needs: build
    if: ${{needs.build.result == 'success'}}
    runs-on: ubuntu-latest
  
    steps:
      - name: deploy job
        run: echo "Deploy Job validated"
name: ci_1

on:
  workflow dispatch:
    inputs:
      environment:
        description: "select environment"
        required: true
        type: choice
        options:
          - dev
          - test
          - prod

jobs:
   config:
     runs-on: ubuntu-latest
     
     steps:
      - name: Show select Environment
        run: |
          echo "Select Environment ${{ inputs.environment }}"
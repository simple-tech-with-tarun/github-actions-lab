name: ci_1

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        node-version:
          - 18
          - 25
          - 45
        
        os:
          - windows-latest
          - ubuntu-latest
        
        include:
          - os: windows-latest
          - node-version: 100

    steps:
      - name: Show Node version
        run: |
          echo "OS: ${{ matrix.os }}"
          echo "Selected Node version: ${{ matrix.node-version }}"
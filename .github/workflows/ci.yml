name: ci_1

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version:
          - 18
          - 25
          - 45

    steps:
      - name: Show Node version
        run: |
          echo "Selected Node version: ${{ matrix.node-version }}"
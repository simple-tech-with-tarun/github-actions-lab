name: Concurrency Lab

on:
  workflow_dispatch:

concurrency:
  group: concurrency-lab
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Start
        run: |
          echo "Starting..."
          sleep 30

      - name: Finish
        run: echo "Finished"
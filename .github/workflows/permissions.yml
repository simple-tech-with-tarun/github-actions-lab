name: Permissions Lab

on:
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Show Token Permissions
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Token is available: ${{ github.token != '' }}"